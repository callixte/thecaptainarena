<h1>Les Armes</h1>

<table>
  <tr>
    <th>Nom</th>
    <th>Bonus d'attaque</th>
    <th>Bonus de défense</th>
    <th colspan="3"></th>
  </tr>
  <% @weapons.each do |weapon| %>
    <tr>
      <td><%= weapon.name %></td>
      <td><%= weapon.attack %></td>
      <td><%= weapon.defence %></td>
      <td><%= link_to 'Voir', weapon_path(weapon) %> </td>
      <td><%= link_to 'Edition', edit_weapon_path(weapon) %></td>
      <td><%= link_to 'Supprimer', weapon_path(weapon), method: :delete, data: { confirm: 'Vraiment?'} %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Ajouter une arme', new_weapon_path %>
